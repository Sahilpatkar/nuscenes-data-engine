# Demo Phase 10 Implementation Plan (storytelling rework of the guided tour)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. **Reviews are BATCHED: one consolidated review after Task 6, the final whole-branch review before push.**

**Goal:** Rewrite the guided tour as *problem → intervention → impact* per the user's brief (`docs/nuscenes_demo_storytelling_improvement_plan.md`) and the spec `docs/superpowers/specs/2026-08-23-demo-phase10-design.md`. App-only; package v0.8 unchanged; the 7-step skeleton, all degradation branches and every honesty anchor survive.

**Architecture:** new tour-scope vocabulary in `app/demo/filters.py` (`arm_story_label`, `tour_strategies`, `relative_gain`, `gain_text`, `upgrade_callout`) and `app/demo/render.py` (`LEGEND_ITEMS`, `legend`), then a step-by-step copy/composition rewrite of `app/demo/views/tour.py` with the legend rolled out to `active_learning.py` / `weak_supervision.py`. Deep pages otherwise untouched.

**Tech Stack:** streamlit 1.59 (`st.expander`, `:color-badge[…]` markdown, `metric_cards` with delta), pandas, AppTest. Strict mypy + ruff. No new dependency.

**Working branch:** `demo-phase-10` (cut from `demo-phase-9b` at 310849d; rebase onto main once PR #23 merges). One commit per task. Baseline: 787 passed / 3 deselected (2 Neo4j-gated skips when the local stack is down).

**Verified facts (2026-08-23 inventory — do not re-derive):**
- `views/tour.py` (1230 lines): `_STEPS` :1088-1147 (7 steps; titles/links per step listed there); `render()` :1184 (`st.title("Guided tour")` :1185, Back/Next :1200-1211, breadcrumb :1224, step caption :1225, bordered container :1226, `_render_go_deeper` :1228, restart :1230). Step renderers: `_render_weakness` :218-263 (3 metric cards :226-236, miss-count line :247-250, the 4-clause paragraph :255-260, 2 provenance :261-263); `_render_missed_pedestrian` :369-406 (radio `tour_hero_model` :380-383 with `format_func=model_label`, HERO_CAPTION caption :399, `_pedestrian_facts` :301-323 fact lines via `_claim_text` :283-299, `_miss_to_hit_sentence` :349-366, NO legend); `_render_mine` :499-543; `_render_why_selected` :572-661 (chips :618-626, seven factor lines :627-631, conditional sentences :635-643, causal sentence :649-653, "reproduced" provenance :655-659); `_render_retrain` :664-726 (4 cards :678-701, the full 13-arm chart :703-721, fairness sentence :722-725); `_render_after` :773-819 (radio `tour_exemplar_model` :785, single overlay, `_OVERLAY_LEGEND` caption :804, `fixed_boxes` dataframe :810, `_night_map_sentence` :729-745, `_HERO_HONESTY_LINE` :124-127); `_render_result` :1069-1081 with the four answers `_result_weakness` :849-877, `_result_data_added` :902-947 (comparator clauses `_mined_comparator_clause` :880-899), `_result_improved` :950-985, `_result_failures` :988-1066. `_GO_DEEPER_PER_ROW = 3` :1168.
- `main.py:37-39` `st.Page(tour.render, title="Guided tour", …)` — stays. Nav sections :84-88.
- `filters.py`: `model_label` :753-762 (`_MODEL_LABELS` covers champion + the two weak arms only); `strategy_coverage` :971-1030 (consumes an ordered {arm: label} mapping — the ready-made mechanism); `selection_factors` :958; `reason_chips` :1161; `fixed_boxes` :825 (`_FIXED_BOX_COLUMNS = [annotation_token, category_group, distance_to_ego_m, baseline_claim, arm_claim]`; claim strings "none" / "low-conf 0.30" / "0.47"); `frame_quota_before` :1108. `active_learning.py`: `_STRATEGIES` :97, `_STRATEGY_NOTES` :105, `_render_strategies` :355-405, the 13-arm charts :409-435, per-arm expander :444; `_OVERLAY_LEGEND` :762 area. `weak_supervision.py`: `_OVERLAY_LEGEND` :114-119 (render :429), pseudo prose legend :103-108 (stays). `render.py`: `STYLE_*` :60-71 (GT green solid w2 / FN orange dash w3 / TP white w1 / low-conf yellow dash w2 / FP red w2 / pseudo blue w2), `metric_cards` :16-30 (delta form), `learned` :673-679 (never used by the tour), `chip_row` :660, `bar_chart` :302, `loop_breadcrumb` :590.
- Arm-table facts (v0.8): score-based arms present are `rate` (+0.0057 night), `strat` (−0.0086), `rate_strat` (+0.0069 — the computed best); `random` −0.0048, `mined` +0.0072, `graph_rate_night` +0.0101 (true max over all 13); all non-baseline arms share `n_train_images` 8,535; night-ped 0.0826 → 0.1171 (+0.0345 abs, +41.8 % rel); `mined` night_share 0.000, `random` 0.127, arm 0.309.
- Fixture facts: hero package night-ped 0.0500 → 0.0900 (→ "+0.0400 absolute, +80.0% relative"); step-4 cards delta "+20 mined frames"; arms present: baseline, random, graph_rate_night (+ weak pairs) — `tour_strategies` renders 3 bars; `_add_weak_night_twin_arm` :3131-area is the row-injection idiom for the §37 test.
- Pinned-test blast radius (tests/test_demo_app.py): title pins :3244 (`test_overview_cta_switches_to_the_tour`) and `_SECTIONED_PAGES` :3729-3736 pin the RENDERED title (`at.title`), not the sidebar label — both change to the new title; `test_tour_walks_steps_0_and_1` :3751 (step captions :3762/:3801/:3828, radio options :3810, HERO substring :3812, fact-line shape :3816-3818, metrics :3771-3775, miss-count :3779); `test_tour_walks_steps_2_to_5` :3852 (captions :3864/:3882/:3895/:3915, factor lines :3885-3886, cards :3898-3907, exactly-1-chart :3908, `tour_exemplar_model` options :3916, 1 image :3917, dataframe `arm_claim` :3919-3920, night sentence :3923); chips test :3935 (survives byte-for-byte); result screen :3961 (caption :3973, four headings :3980-3985, answer sentences :3994-4046, ≥6 links :4053, restart :4058); worst-night-arm :4067 (:4088 survives); hero-honesty :4091 (step-1 pin :4110-4112 changes; :4115-4135 survive); flagship/rejected :4138 (survives); train-pool :4185 (survives); copy-contract :4241 (legend assertions :4266-4267 and :4277-4281's `_OVERLAY_LEGEND in al` drop; the rest survive); deep links :4296 (survives); degradation :4321 (survives); CAN curve :2585 (survives).

---

### Task 1: shared vocabulary & primitives (pure additions)

**Files:** modify `app/demo/filters.py`, `app/demo/render.py`; tests `tests/test_demo_filters.py`, `tests/test_demo_render.py`.

- [ ] Failing unit tests: `arm_story_label("graph_rate_night") == "Graph + night targeting"`, `arm_story_label("weak_graph_rate_night")` falls through to `model_label`'s string, unknown id passes through raw; `tour_strategies` on a frame with all nine arms returns the five ids in order (baseline, random, mined, rate_strat, graph_rate_night) with story labels, skips absent ids (fixture-like 3-arm frame → 3 entries), collapses a duplicate (arm == "mined"), picks the best score-based arm by max `delta_night` (NA rows dropped); `relative_gain(0.0, x) is None`, `relative_gain(0.0826, 0.1171)` ≈ 0.4177; `gain_text` renders the full clause and omits the relative clause when the base is 0; `upgrade_callout` prefers the first pedestrian row, falls back to the first row, returns None on empty; `legend_text()` contains one badge per `LEGEND_ITEMS` entry and `legend_text(pseudo=True)` appends the pseudo item; the drift guard asserts one LEGEND item per `_PRED_STYLES` status plus GT and FN (and PSEUDO ↔ `STYLE_PSEUDO`).
- [ ] RED → implement per spec §1 (place `LEGEND_ITEMS` beside the `STYLE_*` block; `legend()` = `st.markdown(legend_text(...))`) → GREEN; ruff; mypy; full suite (nothing else changes).
- [ ] Commit `demo: tour story vocabulary (arm story labels, tour strategies, gain text, upgrade callout, legend component)`.

### Task 2: title + steps 0–1

**Files:** modify `app/demo/views/tour.py`; tests `tests/test_demo_app.py`.

- [ ] Failing tests (update in the same commit): title pins :3244 and :3730 → `"From model failure to better training data"`; walk test steps 0–1: new captions "Step 1 of 7 · We found a blind spot" / "Step 2 of 7 · What the failure looks like"; subtitle caption substring; step-0 subheader "Aggregate accuracy was hiding a night-driving blind spot."; a `learned` container on step 0; metric cards + miss-count line pins unchanged; step-1 radio options are the two story labels (`["Baseline (no mined data)", "Graph + night targeting"]`); the legend markdown present; the held-out caption present; the bridge sentence; the rephrased fact line (hero-honesty pin :4110 → "Graph + night targeting: 0.135 (low-confidence — below the 0.40 hit floor; the matching rule still counts it as a hit)" — keep `_claim_text`'s no-claim/confident branches consistent).
- [ ] RED → implement spec §2 rows 0–1 (title + subtitle; step-0 rewrite deleting :255-260; step-1 additions; `format_func=arm_story_label`) → GREEN; ruff; mypy; full suite.
- [ ] Commit `demo: tour title + steps 0-1 (blind-spot framing, story labels, legend, held-out caption)`.

### Task 3: steps 2–3

**Files:** modify `app/demo/views/tour.py`; tests `tests/test_demo_app.py`.

- [ ] Failing tests: captions "Step 3 of 7 · Where else does this happen?" / "Step 4 of 7 · What data should we add?"; step-2 headline + mechanism sentence + `learned` container; every existing step-2 pin (scene/severity, within-10-m, parity, night count, CAN charts :2585) unchanged; step-3 headline; the §9 one-liner markdown; an expander labelled "How selection works" containing the factor lines (`"**Night frame:** yes ✓"` etc. still found — AppTest sees expander children) and the causal sentence; chips still render before the expander (chips test :3935 byte-identical); flagship/rejected sentences (:4138) and "reproduced" provenance outside the fold; the architecture-strip caption "System path: nuScenes → validated Parquet → …".
- [ ] RED → implement spec §2 rows 2–3 → GREEN; ruff; mypy; full suite.
- [ ] Commit `demo: tour steps 2-3 (recurring-scenario framing, how-selection-works fold, architecture strip)`.

### Task 4: step 4

**Files:** modify `app/demo/views/tour.py`; tests `tests/test_demo_app.py`.

- [ ] Failing tests: caption "Step 5 of 7 · We changed the training data"; the four metric-card pins (:3898-3907) unchanged; the bold comparison line (fixture: "100% night" vs "0% night" random? — derive from fixture rows: random night_share 0.0, arm 1.0 → pin the derived string); still exactly 1 vega chart whose categories are the story labels present in the fixture (3) with the arm highlighted; the raw-id caption under the chart; the fairness statement (budget clause present in the fixture — all non-baseline arms share `n_train_images`); the computed winner statement naming the fixture's max-`delta_night` arm; a new injected-`mined`-row test (the `_add_weak_night_twin_arm` idiom) for the §37 similarity caption (night_share 0.0 → the caption; absent → not rendered); the scene-diversity sentence with noun agreement.
- [ ] RED → implement spec §2 row 4 (replace :703-725) → GREEN; ruff; mypy; full suite.
- [ ] Commit `demo: tour step 4 as the intervention screen (5-strategy chart, fairness, computed winner)`.

### Task 5: step 5 side-by-side + legend rollout

**Files:** modify `app/demo/views/tour.py`, `app/demo/views/active_learning.py`, `app/demo/views/weak_supervision.py`; tests `tests/test_demo_app.py`.

- [ ] Failing tests: caption "Step 6 of 7 · Did it fix the failure?"; NO `tour_exemplar_model` radio; `len(at.image) == 2` at the step (two overlays) with `arm_story_label` captions; the callout metric cards ("Before — Baseline" / "After — Graph + night targeting" labels with the fixture's claim strings); the `gain_text` sentence (fixture "+0.0400 absolute, +80.0% relative"); the night sentence pin :3923 unchanged; the dataframe inside an expander "Technical details — per-box claims" (`arm_claim` column pin survives); the legend markdown on the step; the held-out caption; hero-honesty pins :4115-4135 and the empty-table branch unchanged; AL and WS render the legend markdown where the prose caption was; copy-contract: the `_OVERLAY_LEGEND` equalities replaced by a source check that none of the three view modules contains the literal "Green = ground truth".
- [ ] RED → implement spec §2 row 5 + §3 (delete the three `_OVERLAY_LEGEND` constants) → GREEN; ruff; mypy; full suite.
- [ ] Commit `demo: tour step 5 side-by-side before/after + legend component rollout`.

### Task 6: step 6 payoff + go-deeper links

**Files:** modify `app/demo/views/tour.py`; tests `tests/test_demo_app.py`.

- [ ] Failing tests: caption "Step 7 of 7 · Closed the loop"; the guarded subheader (fixture `delta_night = +0.0300` > 0 → "… measurable improvement"; add a variant via the existing arms-rewrite idiom with `delta_night < 0` → "… measured result"); the three hero cards (fixture values "20", "100%", "+80.0%"; each omitted on NA — cover with the no-scenes variant if one exists, else assert presence only); answer-3 pins → the `gain_text` phrasing; answer-2 gains the §37 clause under its condition; answer-4 pins (:4025-4046, :4088) unchanged; the closing thesis sentence; every `_STEPS` link label updated to the story form while keeping the page-name prefix (substring pins :3794/:3875/:3932 survive); ≥ 6 links + restart pins unchanged.
- [ ] RED → implement spec §2 row 6 + the link labels → GREEN; ruff; mypy; full suite.
- [ ] Commit `demo: tour result screen payoff (hero numbers, computed headline, story links)`.

**>>> CONSOLIDATED REVIEW GATE (Tasks 1–6): every rewritten sentence recomputed against the shipped v0.8 package; story labels never inside pinned experimental claims; computed branches reachable both ways; step lengths; pinned keys/strings; AppTest coverage. Fix round. <<<**

### Task 7: docs + operational

- [ ] `docs/DEMO.md`: guided-tour section (new title + subtitle, step-title table, retired `tour_exemplar_model`, the legend component, the folds); success-walk rows naming step titles; README only if it quotes them. Commit `demo: Phase 10 docs (story tour)`.
- [ ] Browser walk of all 7 steps on :8599 (Playwright temporarily in `.venv`, kill by port): legend badges render (`:yellow-badge` support), side-by-side fits, folds open, ~one-viewport steps; re-shoot `docs/img/demo-tour.png` (and any other changed shot); stamp affected success-walk rows; commit.
- [ ] `uv run ruff check . && uv run mypy && uv run pytest -q`; final whole-branch review → push `demo-phase-10` → check Actions via the public API → PR compare link (base `demo-phase-9b` until #23 merges).
