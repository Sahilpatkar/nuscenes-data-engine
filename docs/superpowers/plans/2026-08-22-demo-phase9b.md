# Demo Phase 9b Implementation Plan (deep-page hooks, held-out weak-arm predictions, package 0.8)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. **Reviews are BATCHED: one consolidated review after Task 8, the final whole-branch review before push.**

**Goal:** Make each deep page a visual hook — Failure Explorer image-first with the model beside the image, a one-screen Scenario viewer with a real CAN curve and a progressive graph path, an acquisition-strategy comparison with reason chips, and Weak Supervision's "one frame, three views" backed by held-out weak-arm predictions — and ship package v0.8 (`vlm_count_buckets.parquet`, per-step CAN speed, five models). Spec: `docs/superpowers/specs/2026-08-22-demo-phase9b-design.md`.

**Architecture:** pure helpers in `app/demo/filters.py` (`FILMSTRIP_STEPS`, `filmstrip_steps`, `path_steps`, `strategy_coverage`, `reason_chips`, `weak_showcase_token`, `weak_result_token`) and `render.py` (`line_chart`) feed the four views and the tour; the package side adds `can_speed_*` to `demo/events.py`, `export_vlm_count_buckets` to `demo/exporters.py` + `build.py`, two models to `configs/demo.yaml`/`demo infer`, and bumps `_PACKAGE_VERSION` to `"0.8"`. The app never imports the backend (AST test).

**Tech Stack:** streamlit 1.59 (`st.select_slider`, `st.toggle`, `st.sidebar.expander`, `st.columns`), streamlit-agraph, altair via `render.bar_chart`/`line_chart`, pandas; ultralytics/torch only in `demo infer` (already in `.venv`). Strict mypy + ruff. No new dependency.

**Working branch:** `demo-phase-9b` (cut from `main` at 3d977b9, the #22 merge). One commit per task. Baseline: 711 passed / 3 deselected (`uv run pytest -q`, Neo4j-gated tests skip when the stack is down).

**Verified facts (2026-08-22 research brief — do not re-derive):**
- Seams: `views/failures.py` sidebar calls at :93-185 (model radio :99 `failure_model`, Lighting :101, Rain :106, Category :114, Size :119, Distance slider :129 — `distance_arg = None` when the range equals the full extent is load-bearing :149, Failure type :153 `failure_type_select`, Curation bucket :159, Sort by :185); grid :200-214 (`failure_select_{token}` → `st.session_state["failure_token"]`); detail :216-300 (view radio :230 has no key; per-box dataframe :279; fix-pair semantics `fixes_fn_vs_{other}_{model}` :283-291). `views/scenarios.py`: `_BEFORE_STEPS/_AFTER_STEPS/_CURRENT_STEP` :163-165, `_render_parity_line` :195, `_render_graph_panel` :261 (agraph `Config(width=700, height=460, directed=True, physics=True)` :344, `st.columns([2, 1])`, click → meta table else `subgraph_narrative`), `_render_viewer` :401, `_render_ego_panel` :443, `_render_context_panel` :458, `_render_model_panel` :483, `_render_filmstrip` :506-544 (inline step loop reading `getattr(row, col)`, `speed_{col}`, `accel_{col}`, slider `scenario_filmstrip` :525), `render` :573. `views/active_learning.py`: `_render_arm_chart` :220-262 (two `bar_chart`s, provenance, superlatives caption, `_render_night_inversion_callout`, per-arm expander), `_render_selected_frame` :353 (factor lines via `selection_factors`), `render` :608. `views/weak_supervision.py`: `_render_cards` :170, `_render_decomposition` :270, `_render_crowding` :302, `_render_frame` :355, `_render_gallery` :413, `_render_downstream` :460, `render` :552. `views/tour.py`: `_render_mine` :475 (`_render_event_filmstrip(row)` at :501 — the curve goes right after), `_render_why_selected` :547 (chips go before the `selection_factors` loop at :590), `_FILMSTRIP_STEPS` pinned equal to scenarios' tuples by `tests/test_demo_app.py::test_tour_copies_deep_page_wording_verbatim` :2919-2926.
- `render.bar_chart(frame, *, x, y, highlight=None, color_field=None, title="", sort=None, zero_line=True, grouped=False, y_title=None, label_angle=None, mark="bar")` :301 — x is nominal; `mark="line"` exists. No `line_chart`. `chip_row(chips, *, color="blue")` :501; `learned(text)` :507; `draw_overlay(image, gt_boxes, predictions, *, mode, scale, pseudo_boxes=None)` :182; `metric_cards` accepts `(label, value[, delta])`.
- `filters`: `selection_factors(row, *, n_communities, night_floor=None)` :704 returns the 7 `(label, value, flag)` rows; `_PICK_PASS_LABELS` :666; `community_jump(communities, *, before, after)` :630; `fixed_boxes(gt, preds, *, baseline, arm)` :578; `visible_gt(gt, token)` :553; `weak_frame_summary(verdict, vlm_rows, counts_row)` :798; `model_label` table `_MODEL_LABELS = {"champion": …}` :510; `graph_node_label(node)` :411.
- `data.py`: loaders are `@st.cache_data`; optional groups are detected by **file existence**, never `manifest.validation`; graceful-empty loaders return `pd.DataFrame(columns=_EMPTY_*)` (`load_vlm_counts` :275 is the model to copy); `STALE_PACKAGE_NOTE` :22 says ">= 0.6". Subgraph dir is `demo_data/graph_subgraphs/`.
- Package v0.7: `scenario_events.parquet` (126 rows) has `t_minus2/t_minus1/t_plus1/t_plus2` tokens + `speed_t_*` (from `ego_pose.speed_mps`) + `accel_t_*` (CAN `accel_long_min_mps2`); 121 events have 5 steps, 3 have 4, 2 have 3. `demo/events.py` reads canbus with `columns=["sample_token","is_hard_braking","accel_long_min_mps2"]` :155-158 and `_add_neighbors` :224-235 is the single shift loop (`_NEIGHBORS` :61). `canbus.parquet` has `can_speed_kmh` (34,149 rows). Subgraph event JSON: `{edges, nodes, path}`; node `{id, label (Scene|Sample|EgoPose|ObjectObservation|Category|Location), group, meta, on_path}`; EgoPose meta `{accel_long_min_mps2, can_speed_kmh, is_hard_braking, speed_mps}`; object meta has `distance_to_ego_m`, `category`, `visibility`; `path` = list of id lists (segment 0 `[scene, sample, egopose]`, then `[sample, object, category]` per match). Arms: `random` 501 scenes / 0.127 night / Δnight −0.0048; `mined` 219 / 0.000 / +0.0072; `graph_rate_night` 368 / 0.309 / +0.0101 (max); `baseline` has NA `n_scenes`. `al_selection_explain` columns: `sample_data_token, arm, is_night, scene_name, community, community_size, community_night_members, community_mass, community_mass_rank, community_quota, degree, degree_rank_in_community, pick_pass, n_failures_routed, mass_routed`; `frame_manifest.al_selected_by == "graph_rate_night"` on 78 of 250 frames; `weak_verdict` accepted 47 / rejected 31; `weak_labels.parquet` 91 pseudo boxes (`sample_data_token, category_group, x_min, y_min, x_max, y_max, score`); `vlm_counts.parquet` 78 rows (`label_confidence` is a string).
- Builder: `_PACKAGE_VERSION = "0.7"` at `build.py:53`; `run_build` :879-1258 — `export_weak_verifier_by_class` at :972, inputs sweep :1141-1207 hashes "each existing VLM label table"; statuses are only `"absent"`/`"included"` (half-staged groups raise). `demo infer`: `cli.py:1080-1157`, YAML `models.<name>: {run, imgsz}`, weights `mlruns/artifacts/<run>/artifacts/weights/best.pt` :1136, CPU, asserts checkpoint class names == `DETECTION_CLASSES`; writes `predictions.parquet`, `gt_boxes.parquet` (`matched_<model>`), rewrites `frame_manifest.parquet` (`n_preds_<model>`, `fixes_fn_vs_<a>_<b>` for every ordered pair); documented ~15 min for 3 checkpoints. Both weak checkpoints exist locally (6.2 MB each; run names `yolov8n_imgsz640_e20_al-weak_graph_rate_night[_gt]`); torch 2.13 / ultralytics 8.4.96 import in `.venv`. `infer.py:41-44` and `:303-304` docstrings wrongly say torch is absent.
- Autolabel: `src/nuscenes_data_engine/data_engine/autolabel/evaluate.py` — `gt_counts(annotations_path, tokens, visibility_min=None)` :33 (reads `sample_data_token, category_name, visibility_token`), `eval_count_buckets(labels, gt)` :101 (`pd.cut(bins=[-1,0,3,9,inf], labels=["0","1-3","4-9","10+"])`, returns `bucket, n, mae`), `run_eval` filters `parse_status == "ok"` :149 and only renders buckets into `eval_summary.md`. `data/autolabel/labels.parquet`: 5,000 rows, one model (`Qwen/Qwen2.5-VL-7B-Instruct`), `ok` 4,986. Documented MAEs 0.08 / 0.87 / 2.81 / 6.67 (`docs/AUTOLABEL_EVAL.md:162-169`).
- Tests: `built_demo_data` (`tests/test_demo_app.py:478-908`) runs the real `run_build` over hand-written inputs (tokens `v0`, `v1`, `wA`, `wR`; events `s1`, `v1`; models `baseline`/`graph_rate_night`; arms `baseline, random, graph_rate_night, weak_random, weak_random_gt`); canbus fixture already has `can_speed_kmh`; `_add_weak_night_twin_arm` :3131; page helpers `_failures_apptest` :1112, `_scenarios_apptest` :1341, `_active_learning_apptest` :1699, `_weak_supervision_apptest` :1883, `_tour_apptest` :2409, `_walk_to_step` :2503; `test_demo_export.py`: `tiny_inputs` :20, `build_config` :1390, version pin :1417. AST no-backend test :37; requirements allowlist :56.
- Docs: `docs/DEMO.md` package layout :169, builder :116, Failure Explorer :296, Scenario :313, graph :334, AL+Weak :365, screenshots :542, success walk :566, phase table :598; README gallery :39-42, phase line :56.

---

### Task 1: five models — weak-arm checkpoints into `demo infer` (operational + small code)

**Files:** modify `configs/demo.yaml`, `app/demo/filters.py` (`_MODEL_LABELS`), `src/nuscenes_data_engine/demo/infer.py` (two docstrings); tests `tests/test_demo_filters.py`.

- [ ] Failing test: `model_label("weak_graph_rate_night")` / `("weak_graph_rate_night_gt")` return the spec labels; other names pass through unchanged.
- [ ] `configs/demo.yaml models:` += the two `{run, imgsz: 640}` entries with a comment naming the arm, run name and that they are the Phase-9b held-out inference models. Correct the two stale docstrings in `infer.py`.
- [ ] Run `uv run nuscenes-data-engine demo infer` (CPU; expect ~25 min; do NOT run pytest concurrently). Verify on `data/demo_curation/`: `predictions.parquet` has exactly five models; `gt_boxes.parquet` has `matched_<m>` for all five; `frame_manifest.parquet` has `n_preds_<m>` × 5 and 20 `fixes_fn_vs_*` columns; the previous three models' predictions are unchanged row-for-row (compare against the committed `demo_data/predictions.parquet` — report any drift).
- [ ] Report the re-verified hero facts for `5994f34b…` (which of the five models claim the shadowed car / the pedestrian, with conf) and whether every `al_exemplars` token still satisfies "baseline misses, graph_rate_night catches" — these feed Task 9's captions.
- [ ] GREEN; ruff; mypy. Commit `demo: add the two weak-arm checkpoints to demo infer (five models over the held-out val frames)` (config + code only; staging data is untracked).

### Task 2: package-side helpers — per-step CAN speed, `FILMSTRIP_STEPS`/`filmstrip_steps`, `render.line_chart`

**Files:** modify `src/nuscenes_data_engine/demo/events.py`, `app/demo/filters.py`, `app/demo/render.py`, `app/demo/views/scenarios.py` (re-export), `app/demo/views/tour.py` (re-export); tests `tests/test_demo_events.py` (or wherever `build_events` is tested), `tests/test_demo_filters.py`, `tests/test_demo_render.py`, `tests/test_demo_app.py` (copy contract).

- [ ] Failing tests: `build_events` output carries `can_speed_kmh` and `can_speed_t_minus2…t_plus2` equal to the per-scene shifts of canbus `can_speed_kmh` (NA at scene edges); `filmstrip_steps(row)` returns `FilmstripStep` entries in order, drops NA neighbours, flags `is_current`, reads `can_speed_*` and falls back to `speed_* × 3.6` only when the `can_speed_*` fields are absent (a flag on the result says which); `FILMSTRIP_STEPS` equals the five `(column, label)` pairs; `line_chart(...)` returns a `LayerChart`, has a rule layer iff `selected` is given, raises on an unknown `selected`, and keeps the nominal sort order; the copy-contract test asserts `scenarios._BEFORE_STEPS + ((None, _CURRENT_STEP),) + _AFTER_STEPS == tour._FILMSTRIP_STEPS == filters.FILMSTRIP_STEPS`.
- [ ] RED → implement: events.py adds `"can_speed_kmh"` to the canbus column list and `df[f"can_speed_{label}"] = grouped["can_speed_kmh"].shift(k)` in `_add_neighbors`; filters `FilmstripStep` dataclass + helpers; `render.line_chart` per spec §2; scenarios/tour derive their tuples from `filters.FILMSTRIP_STEPS` → GREEN; ruff; mypy.
- [ ] Commit `demo: per-step CAN speed in scenario events, filmstrip_steps + FILMSTRIP_STEPS, render.line_chart`.

### Task 3: Failure Explorer as the hook

**Files:** modify `app/demo/views/failures.py`; tests `tests/test_demo_app.py`.

- [ ] Failing tests (extend `_failures_apptest` cases): `at.sidebar` no longer contains a radio with key `failure_model` but `at.radio(key="failure_model")` exists; `at.sidebar.expander` labelled "Advanced filters" contains the Curation bucket multiselect and Sort by selectbox; with no `failure_token` set the detail renders for the first filtered frame and a caption contains "auto-selected"; setting `failure_token = "v0"` shows `v0`'s detail; `at.radio(key="failure_view_mode")` exists with the three mode labels; the per-box dataframe sits inside an expander labelled "Per-box detail"; every previously pinned string and the `failure_type_select`/`failure_select_*` keys still pass; selecting a model via `failure_model` changes the `n_preds` metric; a filter that empties the set still shows `"No frames match these filters"` and no detail.
- [ ] RED → implement spec §1 (read `st.session_state.get("failure_model", default)` before filtering; instantiate the radio in the detail header with `format_func=model_label`; auto-select rule; `st.columns([3, 2])`; expander) → GREEN; ruff; mypy.
- [ ] Commit `demo: Failure Explorer image-first (model beside the image, advanced-filters fold, auto-selected detail)`.

### Task 4: Scenario Search one-screen viewer + CAN curve (+ tour step 2)

**Files:** modify `app/demo/views/scenarios.py`, `app/demo/views/tour.py`; tests `tests/test_demo_app.py`.

- [ ] Failing tests: selecting the fixture event shows ≥ 2 `vega_lite_chart`s in the viewer whose specs contain the step labels; moving `scenario_filmstrip` to `t-1` changes the rule in both charts (inspect the chart spec's rule datum); exactly one metric row (six `st.metric`s) between the frame and the filmstrip — the old three panels' headings are gone; the compact parity line, `recorded experiment output` caption and `_MODEL_GT_ONLY_NOTE` still render; tour step 2 shows two charts under its filmstrip; an old package without `can_speed_*` (fixture variant that drops the columns from `scenario_events.parquet`) titles the speed chart "ego speed" and raises nothing.
- [ ] RED → implement spec §2 (header line; `st.columns([3, 2])`; `line_chart` pair; merged metric row; filmstrip slider drives `selected`) → GREEN; ruff; mypy.
- [ ] Commit `demo: one-screen Scenario viewer with the CAN speed/accel curve (also on tour step 2)`.

### Task 5: progressive graph path

**Files:** modify `app/demo/filters.py` (`PathStep`, `path_steps`), `app/demo/views/scenarios.py` (`_render_graph_panel`); tests `tests/test_demo_filters.py`, `tests/test_demo_app.py`.

- [ ] Failing tests: `path_steps(event)` on a hand-built event (scene, sample, egopose, two objects at 7.5 m and 2.1 m with categories) returns labels `["1 · Scene …", "2 · Keyframe …", "3 · Pedestrian · 2.1 m", "4 · Car · 7.5 m", "5 · Ego pose …"]` with node ids from `path` and facts including the CAN speed and hard-braking flag; an event with no `path` key → `[]`. AppTest: `at.select_slider(key="scenario_path_step")` options start with "1 · Scene" and default to the last; the node count passed to agraph (monkeypatch `streamlit_agraph.agraph` in the test to capture nodes/edges) equals the on-path count with the toggle off and the full count with `scenario_graph_context` on; at step 1 exactly one node is amber; the step facts table renders; clicking a node (simulate via the captured callback's return) still shows the meta table.
- [ ] RED → implement spec §3 (constant node set; amber / hollow styles via the existing colour constants; context omission; facts table) → GREEN; ruff; mypy; confirm in the browser later (Task 9) that the layout does not jump between steps.
- [ ] Commit `demo: progressive graph path reveal (select_slider + context toggle)`.

### Task 6: Active Learning — strategy comparison + reason chips (+ tour step 3)

**Files:** modify `app/demo/filters.py` (`strategy_coverage`, `reason_chips`), `app/demo/views/active_learning.py`, `app/demo/views/tour.py`; tests `tests/test_demo_filters.py`, `tests/test_demo_app.py`.

- [ ] Failing tests: `strategy_coverage` keeps the mapping order, skips absent arms, carries `n_scenes/night_share/delta_night/n_train_images`; `reason_chips` on the `wA` explain row yields the spec chips in order, omits the pedestrian chip at 0 GT, omits routed failures at 0, includes `quota 1 → 2` when `quota_before` is given, and **no chip contains "rate"**; AppTest: the AL page shows a "Three ways to pick" section with two more charts and a `learned` container whose text interpolates the fixture numbers (fixture has `random` and `graph_rate_night` only → the neutral sentence; `_add_weak_night_twin_arm`-style helper adds a `mined` arm to exercise the computed sentence); the per-frame panel and tour step 3 render a chip row above the factor lines; a frame without an explain row shows the absent note and no chips.
- [ ] RED → implement spec §4 → GREEN; ruff; mypy.
- [ ] Commit `demo: acquisition-strategy comparison + reason chips (AL page and tour step 3)`.

### Task 7: `export_vlm_count_buckets`, build wiring, loader, package version 0.8

**Files:** modify `src/nuscenes_data_engine/demo/exporters.py`, `build.py`, `app/demo/data.py`; tests `tests/test_demo_export.py`, `tests/test_demo_app.py` (fixture).

- [ ] Failing tests: `export_vlm_count_buckets` over a tiny labels table (two models, one truncated row) + fixture annotations returns `model, bucket, n, mae` with ordered buckets and the `n.sum() == n_ok × 5` invariant, skips truncated rows, writes the parquet; `run_build` writes `vlm_count_buckets.parquet` and `validation["vlm_count_buckets"] == "included"` when the Phase-6b table is configured and present, `"absent"` (no file, warning) when not; the labels file is hashed into `inputs`; `manifest["package_version"] == "0.8"`; `data.load_vlm_count_buckets()` returns the table or the empty frame with the four columns.
- [ ] RED → implement spec §5 (reuse `gt_counts`/`eval_count_buckets`; locate the config key the build already uses for the VLM label tables with `grep -n "labels" src/nuscenes_data_engine/demo/build.py configs/demo.yaml` and read the Phase-6b entry from it; fixture `annotations.parquet` gains `category_name`/`visibility_token` if missing; `_PACKAGE_VERSION = "0.8"`, test pin) → GREEN; ruff; mypy.
- [ ] Commit `demo: vlm_count_buckets export (recomputed crowding MAE), loader, package 0.8`.

### Task 8: Weak Supervision — one frame, three views + count-bucket chart

**Files:** modify `app/demo/filters.py` (`weak_showcase_token`, `weak_result_token`), `app/demo/views/weak_supervision.py`; tests `tests/test_demo_filters.py`, `tests/test_demo_app.py` (fixture gains `weak_graph_rate_night`/`_gt` predictions + `matched_*` columns for `v0`, where the `_gt` twin catches the pedestrian the weak arm misses).

- [ ] Failing tests: `weak_showcase_token` picks `wA` (accepted, pseudo box, pedestrian GT) and returns `None` when no frame qualifies; `weak_result_token` picks `v0` (night-first, GT twin fixes a box) and `None` on a package without `matched_weak_*`; AppTest: the page shows "One frame, three views" with six images (two rows of three), `at.radio(key="ws_result_model")` with the pair, the per-box table, the bucket chart (fixture buckets) or `_BUCKETS_ABSENT_NOTE` on the no-buckets variant, the `learned` callout interpolating the four MAEs, the weak-arm fold expander; all existing Weak assertions unchanged.
- [ ] RED → implement spec §6 → GREEN; ruff; mypy; full suite.
- [ ] Commit `demo: Weak Supervision one-frame-three-views (held-out weak-arm predictions) + crowding buckets`.

**>>> CONSOLIDATED REVIEW GATE (Tasks 1–8): honesty of every new sentence/label against the package and the spec's rules (CAN provenance, frame–class pairs, chips without "rate", five-model sentences), derived numbers, degradation on an old package, pinned keys/strings, AppTest coverage, agraph layout stability. Fix round. <<<**

### Task 9: package 0.8 build, docs, screenshots, walkthrough

**Files:** `demo_data/` (rebuilt), `docs/DEMO.md`, `README.md`, `docs/img/*.png`, `scripts/demo_screenshots.py` (only if a page's first screen changed).

- [ ] `uv run nuscenes-data-engine demo build` → assert manifest `package_version == "0.8"`, `validation.vlm_count_buckets == "included"`, the four bucket MAEs round to 0.08 / 0.87 / 2.81 / 6.67, five models in `predictions.parquet`, `can_speed_*` columns present, every exemplar validates, size budget respected; commit the package.
- [ ] Re-verify and rewrite the hero/exemplar sentences that count models (`docs/DEMO.md:186`, `overview.HERO_CAPTION`, tour honesty lines) from Task 1's report; DEMO.md/README updates per spec §7 (phase row 9b shipped, package-layout rows, builder bullets, page sections, success walk, gallery captions, "1–9b").
- [ ] Browser walkthrough (Playwright temporarily in `.venv`, server on :8599, kill by port): Failure Explorer model toggle beside the image; Scenario viewer on one screen with the curve rule following the slider; graph reveal at each step without layout jumps; AL chips + strategy charts; Weak three views; tour steps 2–3. Re-shoot all seven screenshots; commit.
- [ ] `uv run ruff check . && uv run mypy && uv run pytest -q`; final whole-branch review → push → check Actions via the API → PR link.
